[enginx]
enabled  = true
port = all
filter = nginx-error
logpath = /var/log/nginx/error.log
action = iptables-allports[name=enginx, protocol=all]
         blackscript[count=3, mask=16]
ignoreregex = \.(jpg|jpeg|png|gif|js|css)
